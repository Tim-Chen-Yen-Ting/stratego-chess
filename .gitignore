node_modules/
dist/
.env
.env.*
!.env.example

*.tsbuildinfo
.vite/
coverage/
*.log
npm-debug.log*

.DS_Store
Thumbs.db

# Playtest exports. Local research data, not part of the project: they contain
# full deployments, they are large, and they are the input to notebook §6/§9,
# not a build artifact anyone else needs.
games/

#documents
# gamebook.md, techspec.md, fogofwar.md are tracked — the current, canonical
# versions. Old numbered snapshots stay local, same treatment as notebook/plan/
# strategy: research history, not something a GitHub visitor needs.
gamebook_v0*.md
strategy*.md
plan*.md
notebook_v01.md

